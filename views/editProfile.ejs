
  <!DOCTYPE html>
  <html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <!-- Font Awesome -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet" />
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css?family=Roboto:300,400,500,700&display=swap" rel="stylesheet" />
    <!-- MDB -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/mdb-ui-kit/6.1.0/mdb.min.css" rel="stylesheet" />
  </head>

  <body>
    <center>
    <H1>Edit your profile</H1>
    </center>
    <% errMessage?.split(';')?.forEach(element => { %>
      <p style="color: red; text-align: center;"><%= element %></p>
    <% }) %>
    <div class="container ">
      <div class="row">
        <form action="/profile/edit" method="post">

          <div class="form-outline mb-4">
            <input type="text" id="form1Example1" name="name" value="<%= user.name %> " class="form-control" />
            <label class="form-label" for="form1Example1">Name</label>
          </div>
          <div class="form-outline mb-4">
            <input type="text" id="form1Example1" name="imgurl" class="form-control" value="<%= user.imgurl %> " />
            <label class="form-label" for="form1Example1">Image Url</label>
          </div>
          <div class="form-outline mb-4">
            <input type="date" id="form1Example1" name="birtdate" class="form-control" value=<%= user?.birtdateS %> />
            <label class="form-label" for="form1Example1">Birthdate</label>
          </div>
          <div class="form-outline mb-4">
            <select class="form-select" name="gender" aria-label="Default select example">
              <option value="Male" <%=user.gender==='Male'? 'selected':''%>>Male</option>
              <option value="Female" <%=user.gender==='Female'? 'selected':''%>>Female</option>
            </select>
          </div>
          <!-- Submit button -->
          <button type="submit" class="btn btn-primary btn-block">Edit</button>
        </form>
      </div>
    </div>
    <!-- MDB -->
    <script type="text/javascript" src="https://cdnjs.cloudflare.com/ajax/libs/mdb-ui-kit/6.1.0/mdb.min.js"></script>
  </body>

  </html>
